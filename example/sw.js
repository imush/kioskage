// This example's service worker simply loads the shared kioskage worker.
// In your own site you would instead copy js/kioskage-sw.js to your site root
// as sw.js (a worker only controls pages at or below its own path).
importScripts("../js/kioskage-sw.js");
